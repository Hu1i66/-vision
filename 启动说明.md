# RealSense + ArUco / YOLO 启动说明

本文记录当前工作区里如何启动 RealSense 相机，以及基于 ArUco 标定板或 YOLO 的检测节点。

## 1. 进入工作区并加载 ROS 环境

```bash
cd /home/lxf/orange_dataset
source /opt/ros/humble/setup.bash
```

如果你还有自己工作区的 ROS overlay，也一起 source。

## 2. 启动 RealSense 相机

```bash
ros2 launch realsense2_camera rs_launch.py

source /opt/ros/humble/setup.bash && \
ros2 launch realsense2_camera rs_launch.py \
  rgb_camera.color_profile:=640,480,30 \
  align_depth.enable:=true \
  depth_module.profile:=640,480,30 \
  publish_tf:=false

source /opt/ros/humble/setup.bash && \
ros2 launch /home/lxf/agx_arm_ws/launch/eye_to_hand_camera_tf.launch.py


cd /home/lxf/orange_dataset && \
source .venv/bin/activate && \
source /opt/ros/humble/setup.bash && \
python3 realsense_yolo_node.py \
  --ros-args -p show_gui_window:=false \
  -p use_depth_camera:=true

```

正常情况下会看到 `/camera/camera/color/image_raw` 和 `/camera/camera/depth/image_rect_raw` 之类的话题。

## 3. 启动 ArUco 标定板识别

先启动 ArUco 识别节点，使用你给出的单标记 launch：

```bash
source /home/lxf/handeye/install/setup.bash
ros2 launch aruco_ros single.launch.py marker_id:=582 marker_size:=0.1
```

这个节点会发布 `/aruco_single/pose`，检测程序会订阅这个话题并计算机械臂基座坐标。

## 4. 启动检测节点

先激活当前工作区已经配置好的虚拟环境：

```bash
source /home/lxf/orange_dataset/.venv/bin/activate
```

然后运行脚本：

```bash
python /home/lxf/orange_dataset/realsense_yolo_node.py --ros-args -p detection_mode:=aruco -p marker_pose_topic:=/aruco_single/pose -p marker_id:=582 -p marker_size_m:=0.1
```

如果你还要保留 YOLO 模式，可以把 `detection_mode` 改成 `yolo`。

如果你发现基座坐标的正负方向明显反了，先尝试把手眼方向切换一下：

```bash
python /home/lxf/orange_dataset/realsense_yolo_node.py --ros-args -p detection_mode:=yolo -p handeye_direction:=ee_to_cam
```

如果切换后坐标更接近机械臂基座上的真实位置，就保留这个方向；如果没有改善，再切回 `cam_to_ee`。

## 5. 当前环境依赖

这个工作区使用的是现有的 `.venv`，不需要新建虚拟环境。当前已补齐的关键依赖有：

- `numpy`
- `opencv-python`
- `ultralytics`
- `transforms3d`

## 6. 常见问题

如果节点提示找不到包，通常是因为没有激活 `.venv`，或者 ROS 环境没有先 `source /opt/ros/humble/setup.bash`。

如果相机节点提示没有检测到设备，请检查 RealSense 是否已经插好、USB 连接是否正常、以及系统是否能识别到设备。

如果模型加载失败，请确认权重文件存在于：

```bash
/home/lxf/orange_dataset/runs/detect/train/weights/best.pt
```